#!/usr/bin/env pluto

# qualified import
# looks in $PLUTO/libraries and .
import "collections"

str = "Hello"

collections::reverse $str

print $str

# add all "collections" functions into current scope
# if "collections" wasn't already imported, it'd implicitly import it
use "collections"

c = reverse $str

print $str
